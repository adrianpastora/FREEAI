# Optional — omit FREEAI_ADMIN_TOKEN on first install to use the UI setup wizard
# (token stored as hash in Postgres). Provider keys below or in the UI (encrypted in DB).
# FREEAI_ADMIN_TOKEN=

# Optional env fallbacks — same keys as in the control panel
GROQ_API_KEY=
GEMINI_API_KEY=
MISTRAL_API_KEY=
OPENROUTER_API_KEY=
COHERE_API_KEY=
HF_API_KEY=
