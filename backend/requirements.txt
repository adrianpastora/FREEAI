fastapi==0.115.0
uvicorn[standard]==0.32.0
httpx==0.27.2
pydantic==2.10.4
pydantic-settings==2.6.0
python-dotenv==1.0.1
python-multipart==0.0.26
tenacity==9.0.0
cryptography==43.0.3
bcrypt==4.2.1
PyJWT==2.10.1

# database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.13.3

# observability
structlog==24.4.0
prometheus-client==0.21.0

# tokenization — single encoder (cl100k_base) used as a usage / cost
# fallback when an upstream omits its own usage block. Close enough to
# the true BPE for GPT-4/Claude/Llama-3 prose; biased slightly low for
# Gemini/Mistral which is fine since we only use it as a floor.
tiktoken==0.8.0
